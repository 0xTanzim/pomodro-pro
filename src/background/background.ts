// Background script for Pomodoro Pro extension
// Handles alarms, notifications, and background logic

chrome.runtime.onInstalled.addListener(() => {
  // TODO: Initialize storage, alarms, etc.
  // e.g., chrome.alarms.create('pomodoro', { delayInMinutes: 25 })
});

// TODO: Add listeners for alarms, notifications, and messages

export {};
